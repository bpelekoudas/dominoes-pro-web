# Let's write a script to replace the CSS
# We need to change .hand:not(#bottom-hand) .domino to have the same size as bottom dominoes
# Or just remove the scale transforms from the media queries.
# Wait, they are not scaled down on desktop...
# Let's check hand:not(#bottom-hand) .domino
